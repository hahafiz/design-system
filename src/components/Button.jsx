import React from "react";

const Button = () => {
  return (
    <button
      className="px-4 py-2 bg-slate-700 hover:bg-slate-800 rounded-md text-white"
      type="button"
    >
      Buttons
    </button>
  );
};

export default Button;
